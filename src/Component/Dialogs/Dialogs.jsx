import React from "react";
import p from "./Dialogs.module.css"
const Dialogs = (props) => {
    return (
        <div class={p.content}>
            Dialogs</div>

    );
    }
    export default Dialogs;